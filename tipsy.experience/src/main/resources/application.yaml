---
server:
  port: 0
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
     defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
#spring:
#  security:
#    user:
#      name: configUser
#      password: configPassword
#      roles: SYSTEM
spring:
  application:
    name: spring-cloud-eureka-experience
